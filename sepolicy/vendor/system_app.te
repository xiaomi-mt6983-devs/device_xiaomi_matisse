# FM Radio
get_prop(system_app, vendor_mtk_wmt_prop)
get_prop(system_app, vendor_mtk_default_prop)

# Thermal Access for XiaomiParts
allow system_app sysfs_therm:dir search;
allow system_app sysfs_therm:file rw_file_perms;
